services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mitire
      POSTGRES_USER: mitire
      POSTGRES_PASSWORD: mitire
    ports:
      - "5433:5432"
    volumes:
      - mitire-postgres-data:/var/lib/postgresql/data

volumes:
  mitire-postgres-data:
